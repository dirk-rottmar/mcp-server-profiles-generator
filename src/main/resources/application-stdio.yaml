spring:
  application:
    name: mcpprofilesgenerator
  main:
    web-application-type: none
    banner-mode: off
  ai:
    mcp:
      server:
        stdio: true
        name: profilesgenerator-mcp
        version: 0.0.1

# disable banner and logging for stdio transport
logging:
  level:
    root: OFF